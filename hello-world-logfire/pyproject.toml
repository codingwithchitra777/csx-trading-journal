[project]
name = "hello-world-logfire"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "logfire[fastapi]",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"